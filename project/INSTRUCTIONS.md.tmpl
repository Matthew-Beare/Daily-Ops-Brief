# Daily Ops Brief Project Instructions

Use `$ops-brief-policy` for briefs, Ops recovery/maintenance, receipts/orders, and named Ops-list changes. The installed skill and private `Matthew-Beare/Daily-Ops-Brief` repo are canonical.

POLICY_SOURCE_FINGERPRINT: sha256:bda9897dfcfd6aa91ace8ef041839eebbb1090e36eb128f507994e3c0d901296

## Authority

- Timezone: `America/New_York`.
- Ops Sheet: `10WMU_hDMfSJcACel--8LekT7So5MXKgWuLVxvnSCPNU`
- Mileage/pay Sheet: `1OUzdjZaVTidLnMX2xuIZ3mRVDOF5oAfT8-pdl6KfUfI`
- Purchase & Receipt Archive: `1pHkTdCxmdBdZjnVu97FkpkiSjysLkhjuTEcfcEXzmW8`
- Mutable state lives only in those Sheets. If an authority is unavailable: `Action Required — <authority> unavailable`; never substitute memory.

## Fixed automation contract

- Keep exactly one active Ops Brief automation: `2:45 AM/PM Eastern Ops Brief`; `RRULE:FREQ=DAILY;BYHOUR=2,14;BYMINUTE=45;BYSECOND=0`, timezone `America/New_York`. It invokes the skill, selects AM/PM locally, treats PM as morning, and returns only the brief.
- Keep exactly one active `Receipt & Order Lifecycle`: `RRULE:FREQ=DAILY;BYHOUR=1,13;BYMINUTE=45`, Eastern, `timing_mode=exact_schedule`.
- Never create per-order jobs or revive duplicate, 3:00, Pacific, UTC-shifted, child, support, or retry jobs. Scheduled runs never mutate automations.

## Invariants

- A clear named Ops-list change is an immediate canonical Sheet write; preserve `Done`/`Removed` history.
- Read complete Gmail evidence; reconcile `Shipments`; remove delivered items, retain `Order Events`, and report delivery once.
- One purchase has one Receipt ID/total, searchable details, many tags/assets, append-only events, and balanced allocations. Queue unknowns; never guess.
- A true replacement gets a new Receipt ID linked both ways to the original; a same-order revision stays on the original. Never erase cancelled/refunded history or double-count spend.
- Important actionable mail stays under `Ops/Archive Approval` until explicit approval. Never delete Gmail without a bounded request.
- Canonical receipt filing, allocations, vehicle mapping, classification, Shipments, inventory, and reciprocal replacement links must pass the Audit gate before Gmail archival/success.

## Recovery/versioning

- Snapshot automation mutations and roll back failed verification.
- After any lasting policy, schema, workflow, schedule, onboarding, or output change, update tests/fingerprint and automatically commit/push the private repo; never wait for another Git prompt. Never auto-merge, publish publicly, or commit mutable data/secrets.
- Keep this bootstrap below 3,000 characters. Durable detail belongs in the skill/repo. If changed without a direct Project write, return one complete `PROJECT INSTRUCTIONS UPDATE`.
- Never claim a connector or GitHub write succeeded without verification.
