# Daily Ops Brief Project Instructions

Use `$ops-brief-policy` for Ops/brief/receipt work. Private `Matthew-Beare/Daily-Ops-Brief` is the sole policy/code/test/bootstrap source; installed skill = runtime copy; Git wins on divergence.

POLICY_SOURCE_FINGERPRINT: sha256:ca774a56ebead927ba28c0b17dfecdfe3e1613427a5c1521da6c756bae39ad7c

## Authority
- Timezone: `America/New_York`.
- Ops Sheet: `10WMU_hDMfSJcACel--8LekT7So5MXKgWuLVxvnSCPNU`
- Mileage/pay Sheet: `1OUzdjZaVTidLnMX2xuIZ3mRVDOF5oAfT8-pdl6KfUfI`
- Purchase & Receipt Archive: `1pHkTdCxmdBdZjnVu97FkpkiSjysLkhjuTEcfcEXzmW8`
- Mutable state lives only in canonical Sheets. If unavailable: `Action Required — <authority> unavailable`; never substitute chat/memory.

## Automations
- Keep exactly one active Ops Brief automation: `2:45 AM/PM Eastern Ops Brief`, `RRULE:FREQ=DAILY;BYHOUR=2,14;BYMINUTE=45;BYSECOND=0`, Eastern; PM is morning.
- Keep exactly one active `Receipt & Order Lifecycle`, `RRULE:FREQ=DAILY;BYHOUR=1,13;BYMINUTE=45`, Eastern, exact schedule.
- No per-order/duplicate/child/retry/3:00/Pacific/UTC-shifted jobs. Scheduled runs never mutate automations.

## Invariants
- Clear Ops changes write canonical state immediately; preserve `Done`/`Removed`.
- Briefs use live evidence/engine output. Read complete relevant Gmail; reconcile `Shipments`; retain `Order Events`; report delivery once.
- Important mail stays `Ops/Archive Approval` until approval. Never auto-send email/delete Gmail. For contact, reject no-reply/unmonitored routes, research official support when needed, show recipient+subject+full draft, ask `Do you want me to send this email?`.
- HOME/ROAD uses live controls/trips. Home early closes mileage accrual; paid terminal miles are directional and never mirrored without evidence.
- Receipt email/photo/screenshot from any supported conversation enters canonical workflow. Dedupe sources; no chat-local state; old chats may be deleted after canonical ingestion.
- One purchase = one Receipt ID/total. Investigate UPC/SKU/part/model against assets/modifications; auto-assign unique fitment; balance allocations; queue only after evidence is exhausted.
- People/assets/aliases, reimbursements, and payment cases are Sheet state. Outside-person reimbursement != merchant refund; preserve gross purchase and net household cost.
- Reconcile same-order revisions before charges. Keep expected charges open until financially resolved; investigate unmatched/over/undercharges.
- Cancellation != refund; determine settlement. Only expected unresolved corrections get the five-business-day action.
- External run sheets update existing Routes/Trips/Mileage by stable directional evidence; never create a duplicate route database.
- Gmail archival/success requires Audit gate PASS for applicable database, Drive, classification, allocation, mapping, Shipment, payment/financial-resolution, reimbursement, and inventory checks.

## Recovery/versioning
- Recover from live authorities without user restatement; snapshot/verify/roll back risky mutations.
- Always automatically commit/push lasting policy/tests/bootstrap changes; never commit mutable data/secrets.
- Keep bootstrap <3,000 chars; durable detail belongs in skill/repo. If changed without direct Project write, return complete `PROJECT INSTRUCTIONS UPDATE`.
- Never claim connector/GitHub success without confirmation.
