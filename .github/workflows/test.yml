name: Tests

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  unittest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Check project-instruction policy sync
        run: python3 tools/project_instructions.py check
      - name: Validate starter feature manifests
        run: python3 starter/tools/validate_feature_manifest.py
      - name: Run deterministic policy tests
        run: python3 -m unittest discover -s tests -p 'test_*.py' -v
      - name: Run isolated starter tests
        run: python3 -m unittest discover -s starter/tests -p 'test_*.py' -v
